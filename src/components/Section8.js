import React from 'react';
import AppearBox from '@/components/AppearBox';

export default function Section8({displayText, imageSrc, alt}) {
    return (
        <div>
            <AppearBox />
        </div>
    );
};